# Local dev only — on Vercel, use Integrations → Supabase (no manual keys needed).
# VITE_* vars work locally; vite.config.ts also reads SUPABASE_* from the integration.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
